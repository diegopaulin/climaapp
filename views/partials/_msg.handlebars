{{#if success_msg}}
    <div class="alert alert-success">{{success_msg}}</div> <!-- Usando variáveis globais  -->
{{/if}}

{{#if error_msg}}
    <div class="alert alert-danger">{{error_msg}}</div>    <!-- Usando variáveis globais  -->
{{/if}}